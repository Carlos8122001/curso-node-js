const path = require("path")

path.join("/dit","/public") // convierte la ruta en rutas validas para cualquier sistema operativo
path.basename // te indica donde esta el archivo en el O.S
path.dirname // te devuelve la ruta y carpeta donde esta el archivo
